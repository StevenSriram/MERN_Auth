# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets
.env

# ? Ignore GIT from Tracking
    # * git rm -r --cached .env

# ? Update Chages in Commit History
    # * git filter-branch --index-filter "git rm -rf --cached --ignore-unmatch .env" HEAD

# ? Forces to Push Changes
    # * git push --force